This stow setup uses the `--dotfiles` feature for files and folders. In order for this to work, stow 2.4.1 is needed:

- Grab the dist from https://ftp.gnu.org/gnu/stow/
- Untar it
- Run ./configure && make install. Ignore the warnings about `Test::More` and `Test::Output`


